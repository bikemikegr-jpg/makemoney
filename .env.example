# OpenAI API key (or leave blank if using local model)
OPENAI_API_KEY=

# Path for Chroma persistence
CHROMA_PERSIST_DIR=./chroma_db

# Model to use for text generation (default uses OpenAI)
# For local LLMs, set your local inference endpoint or manager
LLM_PROVIDER=openai
LLM_MODEL=gpt-3.5-turbo
